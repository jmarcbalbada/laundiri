<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/login" /> <!-- Background color -->
    <corners android:radius="12dp" /> <!-- Adjust the radius to control the curvature -->

    <stroke
        android:width=".5dp"
        android:color="@color/black" />
</shape>